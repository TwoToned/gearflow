# Policy Exception Register

Temporary, justified deviations from an applicable `MUST`/`SHOULD` rule in
[`POLICY.md`](../POLICY.md), per §15. Every entry **must** have: rule ID, reason, scope,
owner, and an **expiry date** (no expiry = invalid, R-15.1). Exceptions are per-instance,
never blanket (R-15.3). Reviewed at each quarterly sweep (§12); expired exceptions convert
automatically to audit failures (R-15.2).

This is **not** the place for:

- **Budget overrides** → register in the README budget table (R-0.4).
- **Permanent categorical carve-outs** (generated/vendored code) → tool-config exclusions (R-0.5).

> Findings from the baseline audit (`docs/audits/2026-07-18-hygiene-policy-baseline-audit.md`)
> are remediated directly wherever possible; the rows below are the deferrals that need a
> deliberate, dated decision rather than a code fix.

| Rule ID | Reason | Scope | Owner | Expiry |
|---------|--------|-------|-------|--------|
| R-8.1.7 | RVLT brand red (`--red` `#d8353b`) sits at ~4.41:1 against white on the primary CTA (below the 4.5:1 WCAG-AA floor), and worse for red-on-dark text. The brand palette is **accepted as-is** per DESIGN.md rather than re-toned; `color-contrast` is baselined in the axe gates (`e2e/a11y.spec.ts`, `e2e/harness-a11y.spec.ts`) so they stay deterministic and enforce every other WCAG A/AA rule. | `color-contrast` rule only, on the axe a11y gates | Jayden (design) | 2026-10-18 |
| R-8.11.2 | CSP is now split (`next.config.ts`): a zero-risk subset (`base-uri`/`object-src`/`frame-ancestors`) is **enforced**, while the full policy stays **report-only** because its `script-src`/`style-src` `'unsafe-inline'`, `form-action`, and `frame-src` directives can't be enforced without risking SAML SSO (auto-POST to the IdP) and the Google Maps iframes, and no violation-collection endpoint has yet confirmed the allowlist. Full enforcement is deferred until: (a) a CSP report endpoint (a dedicated route — Sentry, the prior CSP-report sink candidate, was removed in the PostHog migration, #650) runs in prod long enough to confirm the allowlist, and (b) the two `'unsafe-inline'` allowances are migrated to nonces. Header presence is asserted by `e2e/security-headers.spec.ts`. | The report-only directives only (the enforced subset above is live) | Jayden (security) | 2026-10-18 |
| R-8.2.2 | `@typescript-eslint/no-explicit-any` stays `warn` rather than lint-banned `error`: 137 existing `any` usages are spread across mature, working code paths, and flipping the rule to error-level in one pass would either fail every PR touching those files or force a large, high-regression-risk retyping sweep with no functional payoff. The gap is covered by a **CI-blocking ratchet** instead of an unenforced aspiration: `scripts/any-ratchet.sh` counts `: any` / `as any` in non-test `src`/`convex` source and fails the build if the count exceeds `.any-ratchet-baseline` (137) — new `any` usage cannot land, and any fix that removes one locks in the drop (the script prints the exact command). This registers that ratchet formally as the interim control until the baseline reaches 0 and the lint rule can flip to `error`. | `@typescript-eslint/no-explicit-any` lint level (`eslint.config.mjs`) + `.any-ratchet-baseline` (currently 137) for `src/**/*.{ts,tsx}` and `convex/**/*.ts`, excluding tests/generated | Jayden (eng) | 2026-10-23 |
| R-8.9.3 | LCP p75 and `convex_op_latency` p95 have been over their alert thresholds continuously since 2026-07-22 (#862, tracked from #873). Triaged 2026-07-25 via direct PostHog SQL against the `convex_op_latency`/`$web_vitals` events (4,388 samples / 5 days) — this is a real signal, not noise or low-volume artifact. Two concrete contributors were found and fixed same-cycle: (1) the public iCal feed route re-ran its full Convex read chain on every external calendar-client poll with no caching — now cached `private, max-age=300`; (2) `RequirePermission` (56 call sites) flashed "Access Denied" for every authorized user while its permissions query was loading, a spurious late LCP/CLS candidate on every gated dashboard — now renders nothing while loading. What's left, and too large for this cycle: (a) a ~480-520ms baseline Convex round-trip latency across nearly every query regardless of shape, consistent with network/infra distance to Convex Cloud rather than a specific query bug; (b) `/crew`, `/warehouse/*`, and `/dashboard` are fully client-rendered (`"use client"`, no SSR/streaming), so first paint of their largest element waits on that round-trip chain — fixing this properly means SSR/streaming those dashboards or batching their Convex reads, a multi-PR architectural change, not a one-off patch. Re-audit after both fixes have a few days of production traffic to confirm the alerts clear or narrow further before deciding whether the residual baseline needs its own follow-up ticket. | LCP p75 (T-7) and `convex_op_latency` p95 (T-P6) alert thresholds only, for the residual client-rendering-architecture / infra-baseline-latency gap — not the two fixed contributors above | Jayden (eng) | 2026-08-25 |
| R-9.12 | T-P4 (#764, extended #831) now tracks per-unit vendor usage (`vendor_usage` PostHog event: Resend sends on **both** send paths — `src/lib/email.ts` direct, `convex/emailActions.ts` scheduled — plus Maps Autocomplete/Place Details requests) but does **not** auto-alert at the 80% line yet, and the "reviewed monthly with a named owner" clause has no operative mechanism yet either. Both gaps share the same root blocker: computing a live ratio (for the alert) and having something to actually look at monthly (for the review) both need either a persistent monthly counter (no Convex deploy credentials were available when #764 landed, and a new Postgres table would be scope creep beyond the Better-Auth/audit models Postgres is limited to) or a PostHog query-capable personal API key (only the write-only ingestion key is configured today) — plus, for alerting specifically, a freed slot under the PostHog plan's already-fully-allocated 5-alert cap (`docs/convex-observability-runbook.md`). As of 2026-07-24 the `vendor_usage` event has not yet accumulated meaningful volume in PostHog, so creating the "Vendor usage (T-P4)" insight now (before there's anything to show) would ship an empty chart nobody reviews — the CWV/crash-free insights precedent this repo follows is "create only after confirming live event volume," not speculatively before. Target reference numbers are pre-computed (Resend 2,400 sends/mo, Maps $12/mo) so whichever blocker clears first can wire the alert and the insight without re-deriving them. | 80%-threshold alerting AND the monthly-review insight/cadence for T-P4 (Maps + Resend); usage tracking itself is live on both Resend paths, not exempted | Jayden (eng) | 2026-10-23 |
| R-8.3.3 | `reservationConflicts.ts`'s `loadOrgGraph()` collects the org's projectLineItems/projectLineItemUnits/assets/projects/models in full (5 sites). Round-3 audit follow-up (#860): a bare `r9.8-ok` code comment isn't a valid exception on its own (R-15.1), and these 5 sites are the highest-traffic offender named in the finding. Real pagination was evaluated and rejected on correctness grounds, not convenience: double-booking conflict detection must compare the inspected project's date window against **every other booking anywhere in the org** — a paginated/bounded read would silently miss conflicts sitting outside the fetched page, which is worse than a slow query for a safety-critical check. One-shot reads only (no reactive subscription — Appendix B), and `requireOrgReadDoc` already bounds every call to a single org. Revisit if/when a project-window-ordered index lets conflict math prune out-of-range projects before the full collect, or if per-org row counts start showing up in slow-query telemetry. | The 5 `.collect()` calls in `loadOrgGraph()` (`convex/reservationConflicts.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `crewDashboard.ts`'s `crewLookups()` (crewMembers/crewRoles/projects, 3 sites) plus the standalone member collect in `stats()` (1 site) — round-3 audit follow-up (#860), same fix as above: replaced the bare comment with a real exception. These are roster/fleet-scale tables (bounded by headcount and active-project count, not by transaction volume the way assignments/time-entries are), fetched to build id→doc join maps for whichever assignment/time-entry subset each caller already narrowed by status via the new `by_organizationId_status` index. As part of this fix, `stats()`/`activeAssignmentsSummary()`/`pendingOffers()` were converted from full assignment/time-entry collects to status-indexed reads (no longer `.collect()`-ing the whole org's assignments/time-entries table at all) — only the roster-scale lookups remain full collects. | The 3 `.collect()` calls in `crewLookups()` plus the member collect in `stats()` (`convex/crewDashboard.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `crewDashboard.ts`'s `allOrgAssignments()` (1 site, shared by `pickerList` and `upcomingShifts`) — round-3 audit follow-up (#860). `crewShifts` has no `organizationId` column, so `upcomingShifts` can only scope its shift lookups via the org's complete assignment-id set; `pickerList` displays each active member's full assignment history (every status, not just active/pending) in the expanded picker view, so a status-narrowed index wouldn't reduce what's fetched — it would just re-fetch every status back out. Revisit if/when `crewShifts` gets a denormalized `organizationId` column (would let `upcomingShifts` query shifts directly instead of via assignment ids) or `pickerList`'s expanded view is redesigned to paginate per member on demand instead of eagerly loading every member's history. | The 1 `.collect()` call in `allOrgAssignments()` (`convex/crewDashboard.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `testtagBackfill.ts`'s `backfillTestTagAssetsCore()` (3 sites: assets/models/testTagAssets) — discovered widening the collect-ratchet to scan `convex/lib/**` (it previously only scanned the top-level `convex/` directory and silently missed this file — #860). Reconciliation (create-missing T&T registrations + retire-orphaned rows) is inherently whole-set: a bounded/paginated read would treat assets or T&T rows outside the fetched page as missing/orphaned and act on them incorrectly (false creates/retires), which is worse than the read cost. Runs inside a browser-direct mutation (model/asset create or update), not on a hot per-request path. | The 3 `.collect()` calls in `backfillTestTagAssetsCore()` (`convex/lib/testtagBackfill.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `webhookEnqueue.ts`'s `enqueueWebhookEvent()` (1 site: `webhooks` table) — discovered by the same `convex/lib/**` ratchet-scope widening (#860). Webhook endpoints are an admin-configured integration table, bounded by how many endpoints an org sets up, not by transaction/event volume — a small platform-scoped table, same class as `crewRoles`/`models`. | The 1 `.collect()` call in `enqueueWebhookEvent()` (`convex/lib/webhookEnqueue.ts`) | Jayden (eng) | 2026-10-25 |
