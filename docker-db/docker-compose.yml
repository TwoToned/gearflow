services:
  db:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: gearflow
    ports:
      - "5432:5432"
    volumes:
      - pgdataNew2:/var/lib/postgresql/data

volumes:
  pgdataNew2:
