// GENERATED FILE — DO NOT EDIT.
// Run `pnpm run mira:feature-map` to regenerate; CI fails if this file is stale.
// Source of truth: ARCHITECTURE.md's FEATUREDOCS table.
// See scripts/generate-mira-feature-map.mts.

export const MIRA_FEATURE_COUNT = 71;

export const MIRA_FEATURE_MAP = "- Tech Stack: Dependencies, env vars, config files\n- Project Structure: Directory layout, all files\n- Database Schema: All Prisma models and relations\n- Auth & Permissions: Better Auth, multi-tenancy, roles, permissions, user customisation\n- Server Actions & API: Server action pattern, all actions, API routes\n- Pages & Layouts: All page routes, layout architecture\n- UI Components: shadcn/ui conventions, custom components, gotchas\n- Assets: Serialized/bulk assets, auto-incrementing tags, categories\n- Kits: Kit system, pricing modes, nested kits, verification\n- Projects: Project management, line items, groups, templates, subhire, custom items\n- Availability: Overbooking engine, reduced stock\n- Warehouse: Deploy/return flows, kit verification, conflict detection, org-wide returns station\n- PDFs: Document generation, nested kit rendering, T&T reports\n- Test & Tag: AS/NZS 3760:2022 compliance module\n- Maintenance: Maintenance records, multi-asset, recurring preventative-maintenance schedules\n- Search: Global search, command palette, @ navigation\n- Notifications: Alert types, dismiss behaviour\n- Media & Storage: File uploads, S3 proxy, photo cascade\n- Mobile & PWA: PWA config, safe areas, barcode scanning\n- CSV Import/Export: Bulk data operations\n- Suppliers: Supplier CRUD, purchase orders\n- Activity Log: Audit trail, change tracking\n- DataTable: Shared table component, filters, column visibility\n- Tags: Universal tags system\n- Settings & Admin: Org settings, branding, site admin, dashboard\n- Patterns: Key conventions, gotchas, code patterns\n- Integration Checklist: What to update when adding new features\n- Maps Integration: Address autocomplete (Nominatim), Leaflet maps, coordinate fields\n- Crew Management: Crew members, roles, skills, certifications. Work-layer phase 4 (#1246) adds the planner's confirmation badges/summary, Triage re-offer/find-cover, bulk \"Request availability…\" with first-come fill, planned-vs-actual, and off-by-default call-time reminders.\n- Preps: Prep-kits (temporary kits), case assets, project staging\n- Enterprise SSO: SAML 2.0/OIDC SSO, provisioning modes, group mapping, enforcement\n- WooCommerce Integration: Webhook-driven order import, client/product matching, location auto-creation\n- Testing: Vitest unit tests, Playwright E2E scaffold, coverage config\n- Check Items: Quality checks, check item library, warehouse integration, close-out\n- Sub-Hires: First-class sub-hire orders with dual pricing, margin analysis, and supplier rate memory\n- Error UX: UserFacingError, Prisma-error translator, showError client helper\n- Custom Fields: Operator-defined entity attributes, definition CRUD, form rendering\n- Cross-Type Equipment Unification: CategorySlot, mixedGroups query, UnifiedAddDialog, SubHireGroupRow, PriceEditDialog, Drop Matrix 8C\n- Child Assets / Accessories: Asset.parentAssetId + AssetBulkChild, SHIPS_WITH/DEDICATED allocation, childKind, project auto-expansion, warehouse cascade, PDF indented render\n- Project Numbering: Configurable auto project codes, %-token template engine, ProjectNumberSequence atomic counter, reset periods, settings UI + live preview\n- Project Tasks: ProjectTask model (status/priority/dueDate/checklist/stage/recurrence/watchers), user-or-crew assignee, project **Work** tab (list/board/calendar), Overview Work card, revived project board, web push subscriptions, getMyOpenTasks cross-project query\n- Bulk Check-In Totals: Convex-native bulk check-in engine (aggregate deployed items by identity, deterministic distribution, over-return rejection) — live caller as of issue #944: the returns station's bulk-tag scan (returnsWrites.returnBulkNative)\n- Real-Time Sync: ⚠️ SUPERSEDED & REMOVED (2026-06-11) — the SSE + Event Bus + RQ-invalidation system was a dead no-op; torn out in the Phase 6 Convex migration. Liveness is now Convex's reactive engine. See [54](./FEATUREDOCS/54-convex-data-layer.md).\n- Convex Data Layer: Convex data layer — all domain reads/writes are native/browser-direct on Convex Cloud; Postgres holds Better Auth + a dormant custom-API pair + a frozen audit log only. Write security bar, service-token read helpers, and the permanent server-action carve-outs.\n- Project Collaboration: Convex-backed collaboration substrate: comment threads (blocking, resolve/reopen, mentions), review markers, group-level blocking gates, live row changed-flash (reduced-motion aware), and a realtime activity feed (incl. grouped imports). Wired on projects + client/supplier/asset records. All state persists to Convex.\n- Agent-Accessible API + MCP: **Reinstatement underway — phases 0-7 landed (#996-#1003).** The API mints a short-lived AGENT token (sub = the key's acting user, akid = the key, never svc) instead of the SERVICE token, so every in-mutation gate applies with no per-operation security code and the requireService-gated functions are unreachable by construction. A real HTTP API is live: POST /api/v1/ops/{operation} (the universal dispatcher), GET /api/v1/operations{,/[operation]}, GET /api/v1/whoami, curated REST aliases, OpenAPI 3.1 + llms.txt — **and a streamable HTTP MCP server at /api/v1/mcp**, 21 curated tools + discovery (list_operations/describe_operation/call_operation) + resources/prompts + a local stdio proxy, all generated from the same registry (pnpm run api:mcp:check, CI-gated). One curated tool, get_project_document (added 2026-07-30), fetches a project's PDFs (delivery docket, pick slip, return sheet, quote, invoice) — it doesn't go through dispatch() since PDF rendering needs Node/Prisma the Convex-only dispatcher can't reach (src/lib/api/documents.ts, also served at GET /api/v1/documents/{projectId}), but its result is JSON like every other tool: a short-lived download URL + metadata, not the PDF bytes inline (an initial base64-blob version broke in real client use and was replaced same-day). **Phase 4 (#1000) added the safety rails**: every agent-reachable mutation carries a CI-gated danger classification, danger:\"high\" requires confirm:true at the dispatcher, the §6 privileged-arg table is fully enforced (project:unlock_pricing, renamed by #1230 from project:unlock_session, denied by default), the noFinancials key flag extends to models/crew-assignment cost fields, the activity log has an agent-authored badge/filter, and revertAgentWindow lets an operator reverse a bad agent warehouse run. **Phase 5's coverage sweep took agent-reachable operations from 331 to 549** (284 queries + 265 mutations) — essentially every requireOrgRead/requireOrgReadDoc call site is now migrated, and the agentOps colocated annotation format (convex/lib/agentOps.ts) records every SERVICE-only-query triage decision (widen/deny) with a reason. **/settings/api-keys** (Phase 6) mints/rotates/revokes keys, four scope presets, the org-wide kill switch, a per-key request log, and a one-click \"Connect an AI Agent\" MCP flow. **Phase 7 (#1003) added MCP OAuth 2.1** — .well-known authorization-server + protected-resource metadata, RFC 7591 dynamic client registration, a session-gated consent screen at /oauth/authorize that narrows requested scopes to the consenting user's live RBAC, a token endpoint (authorization_code+PKCE and refresh_token grants), and RFC 7009 revocation. An OAuth grant is a plain apiKeys row (origin: \"oauth\") — a pure auth adapter with zero new gate logic, unlocking claude.ai + desktop connectors for non-technical staff with no admin involvement. Coverage table: [docs/api-coverage.md](./docs/api-coverage.md).\n- Revenue Allocation & Gear ROI: Splits a kit's or bundle's price across the gear inside it (allocatedRevenue + allocationBasis on every line item), so per-**model** ROI is answerable for gear that only ever ships inside a kit. One recursive walk of the line-item tree, integer-cent largest-remainder rounding, run from the tail of recalcProjectTotals so no write path can forget it. Surfaces: kit allocation panel, model ROI tab, /assets/roi fleet leaderboard. Design: [docs/revenue-allocation-design.md](./docs/revenue-allocation-design.md)\n- Webhooks: Signed outbound events (project.status_changed, line_item.added, warehouse.checked_out, maintenance.created) with HMAC signing, secret rotation, exponential-backoff retries, a delivery log, auto-disable, and a strict SSRF policy. Design: [docs/designs/webhooks.md](./docs/designs/webhooks.md)\n- Bulk Operations: Multi-select + bulk actions across all four project surfaces — **Equipment** (delete/move/edit), **Services** (status/delete), **Crew** (status/remove), **Tasks** (move/priority/delete). Shared useSelection + BulkActionBar + BulkDeleteDialog; batched server actions loop single-item Convex mutations with one recalc + one audit each.\n- Assets on a Job: Project Equipment tab shows which specific serialised assets are prepped/deployed/returned on a job (inline tag for single, expandable per-unit rows + status badge for multi-qty, kit-member tags), with a per-unit \"reassign to another same-model line\" picker to correct the scan auto-pick. RETURNED units are retained through check-in/close-out (+ deprep guard + assetScanLog backstop), so a finished job still shows what went out. Extends [line-item-fulfillment-model](./docs/designs/archive/line-item-fulfillment-model.md).\n- Observability: Analytics + error tracking on PostHog (posthog-js/posthog-node), PII-hardened. Replaced Sentry entirely (#650) — client/server exception capture, Core Web Vitals, and deploy-pipeline sourcemap upload (@posthog/nextjs-config, BuildKit-secret-mounted CLI token) that hard-fails the build if misconfigured rather than silently skipping.\n- Project Lifecycle Locks: **#1230 (Phase 4, \"Project versioning v2\" parent #1221) collapsed the whole 4-tier lock model into one field**: projects.pricingLocked (+ pricingLockedAt/pricingLockedById/pricingLockedByName), applying to the LIVE version only. A quote send or a CONFIRMED status transition raises it; only a person clears it (projectPricingLockWrites.unlockPricingNative, canUnlockPricing = invoice:publish or the project's own PM). Structure/plan/warehouse writes are NEVER gated — only a money-field write against the live version can be rejected (assertPricingUnlocked), and a non-live projectVersions row is writable regardless of the flag. The old JUSTIFY tier (ON_SITE freeform justification), HARD_LOCK (COMPLETED/INVOICED) and unlock sessions (open/commit/discard + snapshot-restore) are deleted outright — see FEATUREDOCS/78's Phase 4 section for the full history.\n- Client Contacts: Multiple contacts per client (#948, WS9) — a new clientContacts child table (fully optional, exclusive isPrimary), a per-project clientContactId picker, and one shared getPrimaryContact resolver used by the client detail page, PDFs, search, and WooCommerce matching. Expand-migrate phase only — the legacy embedded clients.contactName/Email/Phone fields stay live as a read-only fallback; dropping them is a separate follow-up.\n- Incident Reporting: \"Report Issue\" — the in-app successor to the deleted Discord /fault command (#898). Reuses MaintenanceRecord (type=REPAIR, incidentType/incidentSeverity set) rather than a parallel model; flips the reported asset's status immediately (IN_MAINTENANCE / LOST). Available on CHECKED_OUT project lines, the warehouse deploy/return tabs, and the asset detail page. A check-item FAIL now also opens an immediate REPAIR record (alongside the existing 2-of-3-fails predictive trigger, unchanged).\n- Overbookings & Gaps Board: Org-wide two-layer (hard/pencilled) gear + crew-gap risk board (#942, WS3) — /overbookings, six sections (overbooked gear, pencilled collisions, sale stock to procure, services missing crew, unconfirmed crew, crew double-bookings), the confirm-time gate preview, and dashboard chips. Builds on WS2's getProjectWindow (#941, [11-availability.md](./FEATUREDOCS/11-availability.md)).\n- Finance — Quotes, Invoices, Xero: The finance model (#940, WS1) — reverses the earlier \"no Flow-side finance\" stance: RVLT Flow owns Quote (snapshot-on-publish, versioned) + Invoice/InvoiceLine (Flow-numbered, immutable once issued) generation; Xero owns the ledger/payments via a first-class OAuth2 integration (account-coding cascade, draft-invoice push, client contact mapping with duplicate protection). Client payment profiles (FULL_UPFRONT/DEPOSIT_BALANCE) replace the old hand-typed project deposit fields; depositPaid/invoicedTotal are now recalc-derived.\n- Sales Line Items: New LineItemType: \"SALE\" (#950, WS11) — every model sellable, bulk + serialised. Hybrid taxonomy: type: \"SALE\" + saleMode (NEW_STOCK decrements Model.saleStockQuantity, no rental impact; FROM_RENTAL_STOCK disposes of an owned unit — serialised flips to terminal AssetStatus: \"SOLD\" via a reversible \"un-sell\" action, bulk decrements total/available floored at deployed qty). Always pricingType: FLAT, duration: 1, auto-priced from Model.salePrice. New projects.saleRevenue/saleCostTotal P&L buckets + AllocationBasis: \"EXCLUDED_SALE\" (no rental-ROI weight). PDF: SALE badge on quote/invoice, always included on docket/packing-list, excluded from the return sheet. On the project's own equipment tab, a \"Sale · New stock\"/\"Sale · From fleet\" badge (2026-08) makes a SALE line visually distinct from rental EQUIPMENT for the first time — previously the badge existed only in PDFs. The warehouse Pick tab (2026-08) now also surfaces NEW_STOCK sale lines in their own SKU-picked \"Sale items to prepare\" section (salePickedAt checklist field, setSalePicked mutation) — a partial reversal of the original \"no warehouse involvement\" decision; FROM_RENTAL_STOCK sale lines stay fully excluded. Feeds the Overbookings & Gaps board's \"Sale stock to procure\" section, which (2026-08) links back to it and to each named model. A new **Assets → Sales Stock** tab (2026-08, /assets/sales-stock) lists every active model's saleStockQuantity/salePrice with a per-row restock action (modelWrites.adjustSaleStockNative, a thin wrapper over the shared adjustModelSaleStock primitive) for setting the pool in the first place — complementary to, not a duplicate of, the Overbookings board's reactive oversold-only alert.\n- Mira Assistant: A real LLM tool-calling assistant (OpenRouter, per-org key + model) over the SAME agent-accessible dispatch()/MCP surface (FEATUREDOCS/56) an external agent uses, acting as the asking user via a per-(org, user) apiKeys row it provisions itself (miraKeys table, encrypted at rest) — read-only preset by default, full_agent only when an org admin opts in (miraOrgSettings, /settings/mira). High-danger writes stop and ask a human to confirm (CONFIRMATION_REQUIRED); persisted multi-turn conversations (miraConversations/miraMessages).\n- Project Overview Tab: #1061 — the project detail's home tab, and the IA change around it: the page-wide right sidebar (Schedule/Location/Team/Activity) moved into Overview so every other tab renders FULL WIDTH, and the summary strip + conflicts banner came off the top of the page. Leads with a **Readiness** checklist (gear shortage, double-booked assets, unconfirmed/understaffed crew, unpriced lines, stale pricing) that always renders every check — including passing ones, collapsing to one line when all pass — with the fixing action inline on each failing row. Paired **Quote**/**Invoicing** cards carry only the single next action each; the Finance tab stays the owner of the full revision rail, invoice list, payments and P&L. Backed by a new projectReadiness.forProject query reusing the org board's bounded reads, and it collapsed three pre-existing rule duplications rather than adding a fourth.\n- Project Version Switcher: #1080's version-switching program — ?v= header switcher + read-only bar for viewing a past project version (Phase 3, #1093), promoteRevisionNative's scope: \"PROMOTE\" restore (Phase 2, #1089), and the promote dialog + Finance-tab version rail + invoice lineage + labels + deletion that make it a user-facing action (Phase 4, #1097, retiring the old ⋯ Versions panel). One pure mapper (projectSnapshotEntries) turns a snapshot's (or the live state's, via currentEntries — identical shape) entries into Equipment/Labour/Finance DTOs. Tasks/Files stay live and say so; Notes (the project's own text fields) IS captured and shows read-only. Recall-to-edit (Phase 5, #1100) is still unshipped.\n- Org Setup Wizard: Phase C of the multi-tenant/onboarding program (#1063/#1068), now fully built (C1-C6) — /setup, one route hosting all 5 steps as client state, no per-step routing. Step 1 (C1, #1098) is the only blocking screen: naming the org commits it for real (organization.create() → setActive() → Convex membership mirror → seeded currency/tax defaults). Step 2 (C2, #1099), \"where you operate\" — country auto-fills currency/timezone/tax label/tax rate (shown filled-but-editable) and the business-number field's LABEL (ABN/NZBN/VAT number/EIN); country itself is permanent once saved (M6), enforced server-side by silently overriding a changed value back (withImmutableCountry, the PROJECT_UPDATE_IMMUTABLE pattern). Step 3 (C3, #1101) branding. Step 4 (C4, #1102) numbering/document terms/first location. Step 5 (C5, #1103) team invites + gear import. C6 (#1104) adds the dashboard's \"Finish setup\" checklist — every item derived live from real org state, not stored progress.\n- Activation Checklist: D1 of the multi-tenant/onboarding program (#1063/#1069) — the dashboard's \"Get started\" card, beside (never merged with) the setup checklist above: setup is \"configure the company\", activation is \"do the work\". Four milestones (first model → first asset on it → first non-template project → first line item referencing a model on it), every one derived live from convex/activationMilestones.ts — a user who does the work by hand without ever opening the card finds it already ticked. The single persisted bit is the dismissal, its own orgActivationDismissals Convex table (same reasoning as C6's orgSetupDismissals: reusing either that table or notificationDismissals would tie this feature's lifecycle to an unrelated one's).\n- Org Dormancy Guards: B4 of the multi-tenant/onboarding program (#1063/#1096) — abandonment guards for orgs that never activate. A daily sweep (convex/crons.ts → runOrgDormancySweep, HTTP-hop to /api/cron/org-dormancy) re-derives \"never activated\" fresh every tick (1 member, no milestone, no activity) and advances a day 1/3/7/23/29 email ladder, archiving at day 30 — only the \"which stage did we already send\" marker is stored, never the predicate itself. Archived orgs get a single-use self-service reactivation link (/reactivate/<token>, no session required). allowUserToCreateOrganization (src/lib/auth.ts) now also requires a verified email to create an org, to slow the refill.\n- Category Price Rollup & Group Child Disclosure: projectCategories.pricingDisplay — a category prints either a price per line (ITEMISED, the default every pre-feature row reads as) or one derived subtotal on its section header with every line still listed, quantities intact and money columns blank (ROLLUP). Deliberately NOT a priced Project Group: a group HIDES its contents and shows a typed bundle price; a rolled-up category SHOWS all contents and derives sum(lineTotal), never storing it — so recalc/allocation/revenue are untouched (rollup regroups, it never reprices). revealPriceInRollup opts one line (or one collapsed group row) back into printing its own price, still counted in the subtotal, which is why the header amount prints labelled. buildFinanceLines folds the category into ONE sourceType: \"CATEGORY\" invoice line so the document the client holds and the invoice they are billed from are grouped the same way; resolveCategoryLineCode codes it for Xero (category override → org default — a project category has no org-level twin). The sibling half, **group child disclosure** (projectLineItems.showInGroupOnDocs), lets a Project Group — which otherwise collapses to one row and hides everything inside it — list selected members under that row with description + quantity. Those rows NEVER print a price, with no per-member override: the group's bundle price IS the charge, so printing members' figures too would put two contradictory numbers for the same gear on one document. Display-only, so buildFinanceLines is untouched (the group still bills as one line). Warehouse docs ignore the flag — they list every member regardless.\n- Tax Model — Exemption & Per-Line Rate: T3 (#1091), multi-tenant/internationalisation program (#1063/#1066). clients.taxExempt/taxExemptReason hard-short-circuits a project's tax to zero regardless of any rate configured below it. New projectLineItems.taxRate column: per-line override, resolved line.taxRate ?? project.taxRate ?? orgSettings.defaultTaxRate ?? 0 (groups/services have no rate of their own). recalcProjectTotals now persists a tri-state taxStatus (EXEMPT/UNSET/COMPUTED) + a taxBreakdown JSON array grouping the taxable base by resolved rate — UNSET is the ONLY state for \"nothing configured anywhere,\" so an explicit 0% is never confused with an absent rate. TotalsBlock renders \"Exempt\"/\"Rate not set\"/one row per distinct rate accordingly, never a bare $0.00 for an absence. A single-rate project reproduces the pre-T3 flat computation to the cent (the equivalence gate, recalc.test.ts).\n- Project Status Automation: #1160. A job's status is now a consequence of the work, not a chore someone remembers: sending a quote moves it to QUOTED, the first item packed moves it to PREPPING, the last packed item leaving the dock moves it to CHECKED_OUT, and the last item back moves it to RETURNED. One rule table (convex/lib/projectAutoStatus.ts) — the returns station's old private maybeAutoAdvanceProject folded into it, and extended to the project-scoped check-in, which previously didn't advance at all. Forward-only from an explicit from set (a CANCELLED/COMPLETED job is never reopened, re-firing is a no-op), and never INTO COMPLETED/INVOICED — those hard-lock or commit stock, so they stay a human's click. (#1236 later added PAYMENT_SETTLED as the ONE sanctioned rule reaching CONFIRMED — see row 77.) \"Everything deployed\" is a POSITIVE test — no deployable row still has ordered quantity in the warehouse (stillInBuilding), NOT \"nothing is still PACKED\": a partially deployed bulk line rolls up to { status: CHECKED_OUT, prepStatus: PACKED } on its first unit out, and never-prepped gear has no prepStatus at all, so the absence test flipped jobs with gear still on the shelf. \"Deployable\" mirrors the warehouse page's own equipmentItems filter (EQUIPMENT, not a container row, not a sub-hire group wrapper), which is what keeps services, labour, transport and sale lines from holding a job at PREPPING forever. Patches the project directly on the authority of the warehouse gate the calling mutation already passed (a warehouse role has no project:update), reproducing updateStatusNative's counters and a pricingLocked-annotated audit row (the pre-#1230 unlock-session auto-commit it used to also reproduce no longer applies — that mechanism is deleted, see row 78). Per-org opt-out switches (OrgSettings.projectStatusAutomation, absent = ON); every move is announced in-place (send dialog line, warehouse/returns toast) and logged with metadata.autoAdvanceTrigger.\n- The Money Phase — AWAITING_PAYMENT: #1236. The lifecycle used to jump Quoted → Confirmed, skipping the part where most of the waiting happens. AWAITING_PAYMENT is the agreed-but-unpaid phase: accepting a quote or issuing an invoice moves a job into it, and a payment that settles an invoice **in full** moves it to Confirmed. ONE status, not three — \"deposit invoice sent\" and \"deposit paid\" are already facts on the invoices/payments rows (paymentStatus is itself derived), so they render as derived sub-steps (src/lib/project-payment-progress.ts → <PaymentProgressStrip>, shown only at AWAITING_PAYMENT) rather than duplicating payment state onto the project (R-3.1). PAYMENT_SETTLED is the ONE relaxation of FEATUREDOCS/76's \"never automate into CONFIRMED\" — safe because it re-checks the accepted-quote gate (failing CLOSED, since it has nobody to collect a justification from) and takes the same whole-project snapshot the manual path does; a test pins it as the only rule allowed to reach CONFIRMED. **Merge note (#1221 Phase 4 × #1236):** the old 4-tier lock system this row originally described (AWAITING_PAYMENT held OPEN so newVersionNative stayed reachable) is deleted — see row 78. Under the surviving projects.pricingLocked boolean, maybeAutoAdvanceProjectStatus itself defensively raises the lock the moment a job FIRST reaches AWAITING_PAYMENT or CONFIRMED by any path that hasn't already locked it (an invoice-first job with no quote, or accepting a non-live quote's version), so pricing is never left editable once the job is agreed — and newVersionNative stays reachable regardless, because Phase 4 never gates it on pricingLocked at all.\n- Project Versioning v2 — Phases 1-5: Parent #1221 — a SEPARATE, newer program from #70's version switcher above, which it partially supersedes. Phase 1 (#1226): a new projectVersions table (one row per version, replacing the numeric-only revision/liveRevision pair long-term) + optional projects.liveVersionId + versionId/lineageId on the five plan tables, plus a one-time backfill. Phase 2 (#1228): by_projectId → by_versionId on the four plan tables (a deliberate breaking change) + the versionScope.ts read/write helpers + the materializeVersionRowsNative row-cloning primitive. Phase 3 (#1229): the real verb set on the new table — convex/versions.ts's createNative/makeLiveNative/setLabelNative/deleteNative, replacing promote-as-restore with a pointer flip and collapsing the older saveVersionNative/promoteRevisionNative/repriceFromRevisionNative/two delete verbs into one each. Phase 4 (#1230): the 4-tier lock system (including the AWAITING_PAYMENT/quote-sent extensions rows 76-77 originally described) collapses into one projects.pricingLocked boolean, and the unlock-session mechanism (open/commit/discard + snapshot-restore) is deleted outright. Phase 5 (#1231): the UI — one header pill (version-switcher.tsx), one Versions panel (versions-panel.tsx), one VersionStrip (3 states), a Make-live dialog with conflict listing, and the \"composed object\" read path (project-version-compose.ts) so the Equipment tab renders a non-live version as the real, editable page. projectVersionsRead.ts/projectVersionsEquipment.ts and the old switcher's projected-read-only UI are deleted; a write-side gap for NEW equipment/group/category/service inserts on a non-live version (still stamped onto the live version) is documented as the top follow-up.\n- Today — the personal landing page: Work-layer program phase 0.5 (#1242), depends on phase 0's mentions inbox (#1241, row 17). Today replaces Dashboard as the landing page (D10A) and answers \"what do I need to do today, across every job and client\" — composed ENTIRELY from readers/writers that already exist plus the phase-0 notifications table; the one new query, dashboardLists.needsYou, is bounded to the caller's own managed projects (≤24, shared with dashboardLists.home), never an org-wide scan. Overdue/Today/Later bucketing resolves in the ORG's timezone (src/lib/today-buckets.ts), fixing a real bug /my-tasks and the dashboard both still have (they bucket by the browser's local midnight). The work list (myOpenTasks + mentions) is the only live subscription on the page; the day rail and needs-you rail are one-shot, polled on focus + a slow interval (useFocusPolledQuery) — the reactivity posture work-layer.md §10.7/R13 calls for after a single org-wide query cost 4.66 GB/month in production. Peek is a non-modal role=\"complementary\" panel (never Dialog/Sheet — the documented pointer-events-lock footgun). The dashboard's personal \"My work\" zone (MyWorkSection) is deleted; /my-tasks is now a redirect. Snooze, quick-add, subtasks, personal items and stage grouping all wait on phase 1's schema widen, which itself waits on the field research in work-layer.md §17.\n- Client Relationship Layer — timeline, next step, pipeline: Work-layer program phase 3 (#1245), depends on phase 1's spine (#1243, work resource + widened projectTasks). Adds workItemLinks (a work item ↔ client/contact/quote/invoice/service/crewAssignment/asset/lineItem/location join table, since Convex can't index inside an array) and a unified clientTimeline.forClient read model unioning quotes/invoices/payments (finance events, read off the domain rows directly rather than parsed audit-log text), activityLogs (job confirmed/completed), and activityEvents (comments/mentions AND human-logged calls/emails/notes — clientTimelineWrites.ts's new mutations write there too), all keyed off a denormalised activityEvents.clientId/contactId (stamped by collaboration.ts's recordActivity) so the client page's Timeline tab (now the default of six: Timeline · Projects · Contacts · Work · Notes · Files) is an indexed read. \"Next step\" (a follow_up-kind projectTasks row linked via workItemLinks) sits above the tabs; while any of the client's quotes reads SENT (effectiveQuoteStatus, never the raw column) a next step is required, and 24h after a quote send with none logged the quote:nonext signal joins Today's Triage (dashboardLists.needsYou, FEATUREDOCS/79). The client page's 4th hero stat becomes \"since last touch\"; the new /clients/pipeline view filters projects to ENQUIRY/QUOTING/QUOTED/CONFIRMED, sorted by next-step date, shaded amber/error by days-since-touch against org-configurable (currently default-only, no settings UI yet) thresholds resolved server-side in the org timezone (convex/lib/rottingDates.ts). Phase 2's revived drag-drop project board had not merged when this shipped, so the pipeline is its own read-only query + page rather than a mode of project-board.tsx — re-point it there once that board lands, if useful.\n- Customizable Dashboard — the drag-and-resize widget board: #1267 — reverses DESIGN.md's \"no widget boards\" decision (dated, superseded, not silently dropped). /dashboard is now a per-user, drag-and-resize react-grid-layout v2 board behind a \"Customize\" mode toggle; every v1 widget (src/lib/dashboard-widgets.ts's registry) is a zero-new-reads extraction of existing dashboard/Today JSX into its own file (R-3.1) — including three widgets (todayWorkList/todayDayRail/todayNeedsYouRail) that /today/page.tsx itself now also renders, so there's exactly one implementation of each, not two. Persistence is convex/dashboardLayouts.ts (one self-scoped row per (organizationId, userId), modeled on savedTableViews), debounced-on-drag-end from src/hooks/use-dashboard-layout.ts. <DashboardCard> is the one shared shell (drag handle + remove button, edit-mode only) every widget renders inside; mobile bypasses react-grid-layout entirely for a plain stacked column (touch drag-resize is unreliable).";
