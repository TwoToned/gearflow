# Policy Exception Register

Temporary, justified deviations from an applicable `MUST`/`SHOULD` rule in
[`POLICY.md`](../POLICY.md), per §15. Every entry **must** have: rule ID, reason, scope,
owner, and an **expiry date** (no expiry = invalid, R-15.1). Exceptions are per-instance,
never blanket (R-15.3). Reviewed at each quarterly sweep (§12); expired exceptions convert
automatically to audit failures (R-15.2).

This is **not** the place for:

- **Budget overrides** → register in the README budget table (R-0.4).
- **Permanent categorical carve-outs** (generated/vendored code) → tool-config exclusions (R-0.5).

> Findings from the baseline audit (`docs/audits/2026-07-18-hygiene-policy-baseline-audit.md`)
> are remediated directly wherever possible; the rows below are the deferrals that need a
> deliberate, dated decision rather than a code fix.

| Rule ID | Reason | Scope | Owner | Expiry |
|---------|--------|-------|-------|--------|
| R-8.1.7 | RVLT brand red (`--red` `#d8353b`) sits at ~4.41:1 against white on the primary CTA (below the 4.5:1 WCAG-AA floor), and worse for red-on-dark text. The brand palette is **accepted as-is** per DESIGN.md rather than re-toned; `color-contrast` is baselined in the axe gates (`e2e/a11y.spec.ts`, `e2e/harness-a11y.spec.ts`) so they stay deterministic and enforce every other WCAG A/AA rule. | `color-contrast` rule only, on the axe a11y gates | Jayden (design) | 2026-10-18 |
| R-8.11.2 | CSP is now split (`next.config.ts`): a zero-risk subset (`base-uri`/`object-src`/`frame-ancestors`) is **enforced**, while the full policy stays **report-only** because its `script-src`/`style-src` `'unsafe-inline'`, `form-action`, and `frame-src` directives can't be enforced without risking SAML SSO (auto-POST to the IdP) and the Google Maps iframes, and no violation-collection endpoint has yet confirmed the allowlist. Full enforcement is deferred until: (a) a CSP report endpoint (a dedicated route — Sentry, the prior CSP-report sink candidate, was removed in the PostHog migration, #650) runs in prod long enough to confirm the allowlist, and (b) the two `'unsafe-inline'` allowances are migrated to nonces. Header presence is asserted by `e2e/security-headers.spec.ts`. | The report-only directives only (the enforced subset above is live) | Jayden (security) | 2026-10-18 |
| R-8.2.2 | `@typescript-eslint/no-explicit-any` stays `warn` rather than lint-banned `error`: 137 existing `any` usages are spread across mature, working code paths, and flipping the rule to error-level in one pass would either fail every PR touching those files or force a large, high-regression-risk retyping sweep with no functional payoff. The gap is covered by a **CI-blocking ratchet** instead of an unenforced aspiration: `scripts/any-ratchet.sh` counts `: any` / `as any` in non-test `src`/`convex` source and fails the build if the count exceeds `.any-ratchet-baseline` (137) — new `any` usage cannot land, and any fix that removes one locks in the drop (the script prints the exact command). This registers that ratchet formally as the interim control until the baseline reaches 0 and the lint rule can flip to `error`. | `@typescript-eslint/no-explicit-any` lint level (`eslint.config.mjs`) + `.any-ratchet-baseline` (currently 137) for `src/**/*.{ts,tsx}` and `convex/**/*.ts`, excluding tests/generated | Jayden (eng) | 2026-10-23 |
| R-8.9.3 | LCP p75 and `convex_op_latency` p95 have been over their alert thresholds continuously since 2026-07-22 (#862, tracked from #873). Triaged 2026-07-25 via direct PostHog SQL against the `convex_op_latency`/`$web_vitals` events (4,388 samples / 5 days) — this is a real signal, not noise or low-volume artifact. Two concrete contributors were found and fixed same-cycle: (1) the public iCal feed route re-ran its full Convex read chain on every external calendar-client poll with no caching — now cached `private, max-age=300`; (2) `RequirePermission` (56 call sites) flashed "Access Denied" for every authorized user while its permissions query was loading, a spurious late LCP/CLS candidate on every gated dashboard — now renders nothing while loading. What's left, and too large for this cycle: (a) a ~480-520ms baseline Convex round-trip latency across nearly every query regardless of shape, consistent with network/infra distance to Convex Cloud rather than a specific query bug; (b) `/crew`, `/warehouse/*`, and `/dashboard` are fully client-rendered (`"use client"`, no SSR/streaming), so first paint of their largest element waits on that round-trip chain — fixing this properly means SSR/streaming those dashboards or batching their Convex reads, a multi-PR architectural change, not a one-off patch. Re-audit after both fixes have a few days of production traffic to confirm the alerts clear or narrow further before deciding whether the residual baseline needs its own follow-up ticket. | LCP p75 (T-7) and `convex_op_latency` p95 (T-P6) alert thresholds only, for the residual client-rendering-architecture / infra-baseline-latency gap — not the two fixed contributors above | Jayden (eng) | 2026-08-25 |
| R-9.12 | T-P4 (#764, extended #831) now tracks per-unit vendor usage (`vendor_usage` PostHog event: Resend sends on **both** send paths — `src/lib/email.ts` direct, `convex/emailActions.ts` scheduled — plus Maps Autocomplete/Place Details requests) but does **not** auto-alert at the 80% line yet, and the "reviewed monthly with a named owner" clause has no operative mechanism yet either. Both gaps share the same root blocker: computing a live ratio (for the alert) and having something to actually look at monthly (for the review) both need either a persistent monthly counter (no Convex deploy credentials were available when #764 landed, and a new Postgres table would be scope creep beyond the Better-Auth/audit models Postgres is limited to) or a PostHog query-capable personal API key (only the write-only ingestion key is configured today) — plus, for alerting specifically, a freed slot under the PostHog plan's already-fully-allocated 5-alert cap (`docs/convex-observability-runbook.md`). As of 2026-07-24 the `vendor_usage` event has not yet accumulated meaningful volume in PostHog, so creating the "Vendor usage (T-P4)" insight now (before there's anything to show) would ship an empty chart nobody reviews — the CWV/crash-free insights precedent this repo follows is "create only after confirming live event volume," not speculatively before. Target reference numbers are pre-computed (Resend 2,400 sends/mo, Maps $12/mo) so whichever blocker clears first can wire the alert and the insight without re-deriving them. | 80%-threshold alerting AND the monthly-review insight/cadence for T-P4 (Maps + Resend); usage tracking itself is live on both Resend paths, not exempted | Jayden (eng) | 2026-10-23 |
| R-2.5 | Dependabot PR #608 (`typescript` 6.0.3 → 7.0.2, a major-version devDependency bump) sat open past the T-16 3-day trunk-based budget without a recorded reason. Investigated at the 2026-07-25 audit: the PR fails CI (Lint, Hygiene, E2E all red on the bump alone) and a TypeScript major bump is exactly the kind of change R-2.5's own carve-out anticipates needing manual vetting rather than auto-merge. Closed rather than merged broken — re-opened only once someone budgets time to work through the compiler/lint fallout of a TS major bump deliberately, not as a queued Dependabot PR silently going stale again. | PR #608 (`dependabot/npm_and_yarn/typescript-7.0.2`) only — other Dependabot PRs are unaffected and still bound by the 3-day budget | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `reservationConflicts.ts`'s `loadOrgGraph()` collects the org's projectLineItems/projectLineItemUnits/assets/projects/models in full (5 sites). Round-3 audit follow-up (#860): a bare `r9.8-ok` code comment isn't a valid exception on its own (R-15.1), and these 5 sites are the highest-traffic offender named in the finding. Real pagination was evaluated and rejected on correctness grounds, not convenience: double-booking conflict detection must compare the inspected project's date window against **every other booking anywhere in the org** — a paginated/bounded read would silently miss conflicts sitting outside the fetched page, which is worse than a slow query for a safety-critical check. One-shot reads only (no reactive subscription — Appendix B), and `requireOrgReadDoc` already bounds every call to a single org. Revisit if/when a project-window-ordered index lets conflict math prune out-of-range projects before the full collect, or if per-org row counts start showing up in slow-query telemetry. | The 5 `.collect()` calls in `loadOrgGraph()` (`convex/reservationConflicts.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `crewDashboard.ts`'s `crewLookups()` (crewMembers/crewRoles/projects, 3 sites) plus the standalone member collect in `stats()` (1 site) — round-3 audit follow-up (#860), same fix as above: replaced the bare comment with a real exception. These are roster/fleet-scale tables (bounded by headcount and active-project count, not by transaction volume the way assignments/time-entries are), fetched to build id→doc join maps for whichever assignment/time-entry subset each caller already narrowed by status via the new `by_organizationId_status` index. As part of this fix, `stats()`/`activeAssignmentsSummary()`/`pendingOffers()` were converted from full assignment/time-entry collects to status-indexed reads (no longer `.collect()`-ing the whole org's assignments/time-entries table at all) — only the roster-scale lookups remain full collects. | The 3 `.collect()` calls in `crewLookups()` plus the member collect in `stats()` (`convex/crewDashboard.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `crewDashboard.ts`'s `allOrgAssignments()` (1 site, shared by `pickerList` and `upcomingShifts`) — round-3 audit follow-up (#860). `crewShifts` has no `organizationId` column, so `upcomingShifts` can only scope its shift lookups via the org's complete assignment-id set; `pickerList` displays each active member's full assignment history (every status, not just active/pending) in the expanded picker view, so a status-narrowed index wouldn't reduce what's fetched — it would just re-fetch every status back out. Revisit if/when `crewShifts` gets a denormalized `organizationId` column (would let `upcomingShifts` query shifts directly instead of via assignment ids) or `pickerList`'s expanded view is redesigned to paginate per member on demand instead of eagerly loading every member's history. | The 1 `.collect()` call in `allOrgAssignments()` (`convex/crewDashboard.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `testtagBackfill.ts`'s `backfillTestTagAssetsCore()` (3 sites: assets/models/testTagAssets) — discovered widening the collect-ratchet to scan `convex/lib/**` (it previously only scanned the top-level `convex/` directory and silently missed this file — #860). Reconciliation (create-missing T&T registrations + retire-orphaned rows) is inherently whole-set: a bounded/paginated read would treat assets or T&T rows outside the fetched page as missing/orphaned and act on them incorrectly (false creates/retires), which is worse than the read cost. Runs inside a browser-direct mutation (model/asset create or update), not on a hot per-request path. | The 3 `.collect()` calls in `backfillTestTagAssetsCore()` (`convex/lib/testtagBackfill.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `webhookEnqueue.ts`'s `enqueueWebhookEvent()` (1 site: `webhooks` table) — discovered by the same `convex/lib/**` ratchet-scope widening (#860). Webhook endpoints are an admin-configured integration table, bounded by how many endpoints an org sets up, not by transaction/event volume — a small platform-scoped table, same class as `crewRoles`/`models`. | The 1 `.collect()` call in `enqueueWebhookEvent()` (`convex/lib/webhookEnqueue.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | Round-4 audit follow-up (#901): the below rows convert bare `r9.8-ok` code comments (present but not backed by a real exception, R-15.1) into registered exceptions, verified against each query's actual code and callers rather than trusting the existing comment text. `crew.ts`'s `orgSkillMap()` (crewSkills), `memberDetail()` (crewRoles), `memberExtras()` (crewMembers), and `orgUsersForCrewLink()` (members + crewMembers) — 5 sites. All are roster/role-scale tables bounded by org headcount or a handful of admin-defined roles/skills, not transaction volume — fetched to build id→doc join/enrichment maps, same class already accepted for `crewDashboard.ts`'s `crewLookups()`. | The 5 `.collect()` calls in `orgSkillMap()`, `memberDetail()`, `memberExtras()`, and `orgUsersForCrewLink()` (`convex/crew.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `crewAssignments.ts`'s `projectCrew()` (crewMembers + crewRoles) and `membersForAssignment()`'s crewMembers/crewRoles lookups — 4 sites (#901). Roster/role-scale lookups (bounded by headcount and a handful of configured roles), used to resolve names/colors for whichever assignment subset each caller already narrowed. The growable-table collects in the same functions (crewAssignments, projects) are NOT covered here — tracked separately as a bounding follow-up (existing `by_crewMemberId`/`by_cuid` indexes apply). | The 4 `.collect()` calls in `projectCrew()` and `membersForAssignment()` resolving crewMembers/crewRoles (`convex/crewAssignments.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `crewAvailability.ts`'s `orgMemberIds()` (crewMembers, shared helper) and `plannerData()`'s crewMembers/crewRoles lookups — 3 sites (#901). Roster/role-scale, same class as the crew.ts/crewAssignments.ts rows above. The date-range-filterable collects in the same function (crewAssignments, crewAvailabilities, projects) are NOT covered — `plannerData` already receives `startMs`/`endMs` and is tracked as a bounding follow-up (range-scan by date instead of collect-then-filter). | The 3 `.collect()` calls resolving crewMembers/crewRoles in `orgMemberIds()` and `plannerData()` (`convex/crewAvailability.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `assetAccessories.ts`'s `availableSerialized()` models collect (1 site, #901). Equipment-model/SKU catalog, admin-authored and catalog-scale — not fleet-instance-scale. The sibling `assets` collect in the same function is NOT covered — tracked as a bounding follow-up (needs a `by_organizationId_status` composite index). | The 1 `.collect()` call on `models` in `availableSerialized()` (`convex/assetAccessories.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `checkItems.ts`'s `list()` (1 site) and `checkRecordWrites.ts`'s `insertCheckRecords()` (1 site) — #901. `checkItems` is an admin-authored check/inspection catalog (test-and-tag/safety-check entries), bounded per org to a handful of rows, not transaction volume. | The 1 `.collect()` call each in `checkItems.ts` `list()` and `checkRecordWrites.ts` `insertCheckRecords()` | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `groupTemplateItems.ts`'s `list()` and `groupTemplates.ts`'s `list()` — 2 sites (#901). Both are admin-authored equipment-kit-template catalogs (parent template + its line items), bounded per org to a handful of templates. | The 1 `.collect()` call each in `groupTemplateItems.ts` `list()` and `groupTemplates.ts` `list()` | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `notificationDismissals.ts`'s `list()` (1 site, #901). Bounded by the finite (users × notification-key) combination space per org — inherently small, not a growing transaction log. | The 1 `.collect()` call in `list()` (`convex/notificationDismissals.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `serviceTemplates.ts`'s `list()` (1 site, #901). Admin-authored service-line-template catalog, bounded per org to a handful of rows. (`sectionPresets.ts` — the other table originally in this row — was deleted with the PDF customization-engine rip-out, #790.) | The 1 `.collect()` call in `serviceTemplates.ts` `list()` | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `wooCommerceIntegrations.ts`'s `list()` (1 site, #901). Effectively a per-org integration-config singleton, same class as the registered `webhookEnqueue.ts` row above. | The 1 `.collect()` call in `list()` (`convex/wooCommerceIntegrations.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `wooCommerceInternal.ts`'s `listLocationsByOrg()` and `listModelsByOrg()` — 2 sites (#901). `locations` is an org's own depots/venues (bounded to dozens); `models` is the SKU catalog (admin-authored, catalog-scale). The sibling `listClientsByOrg()` in the same file is NOT covered — `clients` is a live customer database with real growth; tracked as a bounding follow-up (swap to the existing `collectCapped()` helper). | The 2 `.collect()` calls in `listLocationsByOrg()` and `listModelsByOrg()` (`convex/wooCommerceInternal.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `categoriesWrites.ts`'s `removeNative()` (1 site, #901) collects the org's `categories` table as a delete guard (checking for child categories). `categories` is a small, admin-managed per-org catalog, bounded by how many categories an org defines. | The 1 `.collect()` call in `removeNative()` (`convex/categoriesWrites.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `locationsWrites.ts`'s `unsetOtherDefaults` and `removeNative`'s children check — 2 sites (#901) collect an org's `locations` in full: (a) clearing other default flags and (b) checking for child locations. Bounded by how many physical sites a hire company operates, single digits to low dozens, not by transaction volume. The same function's `removeNative` also collects the entire `assets`/`bulkAssets` tables as a delete guard — NOT covered here, tracked as a bounding follow-up (both tables already have an unused `by_locationId` index for an O(1) existence check). | The 2 `.collect()` calls in `unsetOtherDefaults` and `removeNative`'s children check (`convex/locationsWrites.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `crewTimeEntries.ts`'s `allEntries()` and `forMember()` (5 sites: crewMembers/crewRoles/projects collects, #901) build id→doc join maps to attach project/role/member context to a page of time entries. Roster/fleet-scale tables (bounded by headcount and active-project count), same reasoning already registered for `crewDashboard.ts`'s `crewLookups()`. The full `crewTimeEntries`/`crewAssignments` collects in the same functions are NOT covered — tracked as a bounding follow-up (transaction-volume-scaled, needs a date-range index). | The 3 `.collect()` calls in `allEntries()` (crewMembers/crewRoles/projects) plus the 2 in `forMember()` (crewRoles/projects) (`convex/crewTimeEntries.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `dashboardCounters.ts`'s `computeCounters()` (5 sites: assets/bulkAssets/projects/crewMembers/crewAssignments, #901) recomputes the six dashboard counters from source for `reconcile`. Several of these tables are growable, but this function is not on the hot path — counters are kept fresh in-transaction on every counted write (`convex/lib/counters.ts`), and the client throttles `reconcileIfStale` to a long window, so this only runs as a periodic drift backstop — same shape as the registered `testtagBackfill.ts` exception. | The 5 `.collect()` calls in `computeCounters()` (`convex/dashboardCounters.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `kitBulkItems.ts`'s `list()` and `kitSerializedItems.ts`'s `list()` — 2 sites (#901), used by `getKitBulkItemsByOrg`/`getKitSerializedItemsByOrg`/`countKitMembers` to replace a groupBy-style per-kit member count. Kit composition is bounded by admin-driven catalog size (kit count × members per kit), not rental/transaction volume. `kitSerializedItems.ts:list()` was briefly (and incorrectly) treated as sole-caller-is-the-GDPR-sweep during the #901 BOUND triage — a real second caller (`src/lib/kits-read.ts`'s `getKitSerializedItemsByOrg`) was missed, same class of miss as the `projectMedia.ts` one earlier in this remediation; both tables also gained a `listByAddedById` query (existing `by_addedById` index) so the GDPR sweep no longer needs this `list()` at all. | The 1 `.collect()` call each in `list()` (`convex/kitBulkItems.ts`, `convex/kitSerializedItems.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `locations.ts`'s `list`/`listSimple`/`detail` (4 sites: `locations` self-collect ×3, `models` name-map ×1, #901). `locations` is confirmed small (tens of rows per org); the `models` read is a bounded catalog name-map for display. The `counts()` function's 3 org-wide assets/bulkAssets/kits collects in the same file are NOT covered — tracked as a bounding follow-up (loop location ids via the existing `by_locationId` index, mirroring this file's own `detail()`). | The 3 `.collect()` calls on `locations` (`list`/`listSimple`/`detail`) plus the 1 `models` collect in `detail` (`convex/locations.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `modelMedia.ts`'s `list()` and `assets.ts`'s `registryPhotos` (its `modelMedia` half) — 2 sites (#901) each build an org-wide primary-media lookup map over `modelMedia`. Bounded by model-catalog size × a handful of media rows each, and genuinely needs the full set to build a complete map (a partial read would silently miss primary photos for models outside the fetched page). `registryPhotos`'s `assetMedia` half is NOT covered here — that one is fleet-scale and was fixed for real via a new `by_organizationId_isPrimary` index (round-4 follow-up, #901). | The `.collect()` call in `list()` (`convex/modelMedia.ts`) and the `modelMedia` collect in `registryPhotos` (`convex/assets.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `orgExport.ts`'s `listOrgIds()` (1 site, #901) collects the global `organizations` tenant-count table so the org-export script can default to the single org (and fail loudly otherwise). Admin/script-only, never invoked from a user-facing path. | The 1 `.collect()` call in `listOrgIds()` (`convex/orgExport.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `savedTableViews.ts`'s `list()` (1 site, #901) collects an org's saved table-view configs — bounded by (users × tables × views-per-user), a small UI-preference table with no transactional growth path. | The 1 `.collect()` call in `list()` (`convex/savedTableViews.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `siteSettings.ts`'s `list()` (1 site, #901) collects the global (not even org-scoped) site-settings table — a platform-wide singleton config row, admin/service-only. | The 1 `.collect()` call in `list()` (`convex/siteSettings.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `warehouseDashboardTokens.ts`'s `list()` (1 site, #901). Admin-configured warehouse-display tokens, bounded by how many physical display tokens an org creates — same class as the registered `webhookEnqueue.ts` row. | The 1 `.collect()` call in `list()` (`convex/warehouseDashboardTokens.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | Four near-identical generated-CRUD `list()` queries each collect an org's rows in full (#901): `kitCheckItems.ts`, `modelCheckItems.ts`, `projectCategories.ts`, `projectServices.ts`. All four are admin-authored config/catalog tables (kit/model check-item assignments, project categories, project services) — bounded by how much an org manually configures, not by transaction or booking volume, same class as `crewRoles`/`models`. (`brandTemplates.ts`/`documentTemplates.ts` — two other tables originally in this row — were deleted with the PDF customization-engine rip-out, #790.) | The 1 `.collect()` call each in `list()` across `kitCheckItems.ts`, `modelCheckItems.ts`, `projectCategories.ts`, `projectServices.ts` | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `bulkAssets.ts` collects an org's rows in full at 5 sites (#901): `list()` (used only by server-side cross-domain join helpers `getBulkAssetsByOrg`/`getMappedBulkAssetsByOrg` — the reactive UI path migrated to `listPage`), and `listPage()`'s own bulkAssets/models/categories/locations enrichment collects. `bulkAssets` is a distinct-SKU/bulk-tag catalog (bounded by how many bulk-tracked product lines an org registers, not by unit quantity — quantity lives in `totalQuantity`/`availableQuantity` fields, not extra rows); models/categories/locations are config/catalog tables of the same bounded class as `crewRoles`. | The 5 `.collect()` calls in `list()` and `listPage()` (`convex/bulkAssets.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `clientMedia.ts:list()`, `subHireMedia.ts:list()`, and `projectMedia.ts:list()` — 3 sites (#901) collect an org's media rows in full. All three are consumed only by `src/lib/media-read.ts`'s generic `MEDIA_SPECS`-driven `getPrimaryPhotoMaps` (dispatched per media kind, including "project"), which needs every PHOTO/isPrimary row to build a primary-photo map for client/sub-hire/project list views — a real aggregation, not a per-row lookup, already documented in `media-read.ts` as an interim tradeoff pending a hand-maintained indexed query. `projectMedia.ts:list()` was briefly (and incorrectly) deleted as dead code during the #901 BOUND/DEAD-CODE triage — a direct grep for `api.projectMedia.list` found no literal callers, missing the indirect call through `MEDIA_SPECS[k].convex.list` in `media-read.ts:83`; CI's Type Check caught the regression before merge. Bounded by (clients, sub-hires, or projects) × (media rows per parent), not transaction volume. | The 3 `.collect()` calls in `list()` (`convex/clientMedia.ts`, `convex/subHireMedia.ts`, `convex/projectMedia.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `projectServicesWrites.ts`'s `generateServicesNative()` and `createServiceTemplateNative()` — 2 sites (#901) collect an org's `serviceTemplates` in full, an admin-authored template catalog (a handful of service types), used once each to build a sorted in-memory list / compute the next `sortOrder`. | The 2 `.collect()` calls in `generateServicesNative()` and `createServiceTemplateNative()` (`convex/projectServicesWrites.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `projectTasks.ts`'s `assignees()` (2 sites: members, crewMembers, #901) collects both tables in full to build the task-assignee picker's id→doc join maps. Both are roster-scale tables bounded by org headcount / active crew count, same reasoning already registered for `crewDashboard.ts`'s `crewLookups()`. The unrelated `list()` in the same file has zero callers anywhere in the repo and is tracked for deletion, not registered here. | The 2 `.collect()` calls in `assignees()` (`convex/projectTasks.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `testTagAssets.ts` has 3 sites that hold up (#901): `list()` is consumed only by background T&T reminder/report jobs needing the full per-org set to find overdue items — off the request hot path, same shape as the registered `testtagBackfill.ts` reconciliation row. `listPage`'s and `detail`'s `testProfiles` collects are each a genuinely small per-org config set (a handful of test profiles). The other 9 sites in this file (asset/bulkAsset/testTagRecords enrichment in `listPage`/`dashboardStats`) are NOT covered — tracked as a bounding follow-up (existing `listByIds`/`by_organizationId_status`/`by_organizationId_testDate` indexes apply). | The 3 `.collect()` calls in `list()`, `listPage()`, and `detail()` (`convex/testTagAssets.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `users.ts`'s `listAll()` (1 site, #901) collects the entire platform-wide `users` mirror table. SERVICE-only, used exclusively by the auth-mirror reconcile job to detect orphaned mirror rows and field drift — the same reconciliation shape already registered for `testtagBackfill.ts`: a bounded/paginated read would treat users outside the fetched page as orphans and act on them incorrectly. Not on any request hot path. | The 1 `.collect()` call in `listAll()` (`convex/users.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `availability.ts`'s `calendarData()` (1 site, #901) collects the org's full `projects` table to build the equipment-availability calendar popover — a one-shot browser read (no live subscription). No index lets a rental-window overlap test happen without first pulling candidate projects into JS; per-project line-item counts and client names are resolved afterward via scoped, org-re-checked point-reads. | The 1 `.collect()` call in `calendarData()` (`convex/availability.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `categories.ts`'s `list`, `counts` (models/kits tallies), `detail`'s own `categories` collect, and `containerAssetSearch`'s `categories`/`models` collects — 6 sites (#901). All read small, admin-configured catalog tables (bounded by configured category/model/kit count) or need the full set to compute a tally/tree with no per-row shortcut. Excludes `detail`'s `assets`/`modelMedia` collects and `containerAssetSearch`'s `assets` collect — transaction/inventory-scale, tracked separately as a bounding follow-up (resolve category's model ids via `models.by_categoryId`, then batch-point-read). | The 6 `.collect()` calls in `convex/categories.ts` — `list`, `counts` (×2), `detail`'s own collect, `containerAssetSearch`'s `categories`/`models` collects (×2) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `crewMembers.ts`'s `list` and `listPage`'s crewMembers/crewRoles collects — 3 sites (#901). `crewMembers` is genuinely roster-bound (headcount, not transaction volume), same class already registered for `crewDashboard.ts`. `listPage` is the shipped, tested pagination replacement for the old client-side whole-org subscriptions. | The 3 `.collect()` calls in `convex/crewMembers.ts` (`list`, `listPage` ×2) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `kitMedia.ts`'s `list()` (1 site, #901), called by `getPrimaryPhotoMaps` to build an org-wide kit→primary-photo map in one shot — a genuine full-set aggregation over a catalog-scale table (a handful of media rows per kit). | The 1 `.collect()` call in `list()` (`convex/kitMedia.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `maintenanceRecords.ts`'s `scrubUserRefs()` (1 site, #901) — a GDPR erasure sweep; `maintenanceRecords` has no `by_userId`-style index, so a full-org scan is the only path. Rare (fires only on account deletion). The other 3 sites in this file (`list`, `recordsPage`, `assetsForSelect`) are NOT covered — tracked as a bounding/follow-up item (fingerprint-only reactive read, collect-then-slice, and an unbounded picker respectively). | The 1 `.collect()` call in `scrubUserRefs()` (`convex/maintenanceRecords.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `projects.ts`'s `listPage`'s clients/locations enrichment (2 sites) and `listBoard`'s clients enrichment (1 site) — #901. Small catalog tables used only for id→doc enrichment maps. The core `projects` collects in `list`, `listPage`, and `listBoard` in the same file are explicitly NOT covered — `projects` is this app's highest-traffic growable table, and the perf-design doc itself documents these as open, unshipped follow-ups (not accepted tradeoffs); tracked separately, not registered. | The 3 `.collect()` calls on clients/locations enrichment (`convex/projects.ts` — `listPage` ×2, `listBoard` ×1) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `testProfiles.ts`'s `list` and `resolveForAsset`'s org-wide fallback collect — 2 sites (#901). Small, admin-configured catalog (test templates per equipment-class/appliance-type), not a per-transaction table; `resolveForAsset`'s collect is reached only after three cheaper cascade steps miss. | The 2 `.collect()` calls in `convex/testProfiles.ts` (`list`, `resolveForAsset`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `testTagRecords.ts`'s `list()` (service-only, 1 site, #901) is called only by `getTestTagRecordsByOrg`, which feeds a permanent server-action CSV/compliance-report generator (`src/server/test-tag-reports.ts`). Compliance reports genuinely need the org's full test-and-tag history, off the hot path. | The 1 `.collect()` call in `list()` (`convex/testTagRecords.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `clients.ts`'s `list` (reactive client-catalog subscription), `listPage` (browser-native filter/sort/paginate per `docs/designs/perf-convex-efficiency-2026-06.md` Finding #1), and `projectCounts` (per-client project tallies, aggregation) — 3 sites (#901). `clients` is a catalog table (one row per customer entity), bounded by business-entity count, not transaction volume. | The 3 `.collect()` calls in `convex/clients.ts` (`list`, `listPage`, `projectCounts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `crewRoles.ts`'s `list()` (1 site, #901) — small admin-configured department/role catalog, bounded by headcount-role diversity not transaction volume, same class as the registered `crewDashboard.ts` roster-scale exception. | The 1 `.collect()` call in `list()` (`convex/crewRoles.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `kits.ts`'s `list` (reactive kit-catalog subscription), `listPage` (browser-native filter/sort/paginate + its bounded categories/locations enrichment), `counts` (per-kit serializedItem/bulkItem/media tallies — aggregation, no denormalized counter exists), and the `models` enrichment maps in `availableAssets`/`availableBulkAssets` — 9 sites (#901). The 2 `assets`/`bulkAssets` collects in the same two picker functions are NOT covered — tracked as a bounding follow-up (needs a `by_organizationId_status` composite index on assets/bulkAssets). | The 9 listed `.collect()` calls in `convex/kits.ts` (excludes the 2 assets/bulkAssets picker collects) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `members.ts`'s `listAll()` (1 site, #901). Sole caller is the offline auth-mirror reconcile script — reconciliation is inherently whole-set (an orphan by definition isn't scoped to one org); runs as a rare operator-invoked script, not a request-path query. | The 1 `.collect()` call in `listAll()` (`convex/members.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `models.ts`'s `list` (reactive model-catalog subscription), `counts` (per-model asset/bulkAsset/media tallies — aggregation), and `detail`'s locations/models enrichment maps (bounded catalog joins) — 6 sites (#901). Same reasoning class as the `kits.ts` row above. | The 6 `.collect()` calls in `convex/models.ts` (`list`, `counts` ×3, `detail` ×2) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `testTagAuditorTokens.ts`'s `list()` (1 site, #901) — admin-created auditor-portal tokens, a handful per org, comparable to `webhooks`/`crewRoles`. | The 1 `.collect()` call in `list()` (`convex/testTagAuditorTokens.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `webhooks.ts`'s `list()` and `activeSubscriptions()` — 2 sites (#901). Admin-configured integration endpoints, small per-org set — the same `webhooks` table and reasoning already registered above for `webhookEnqueue.ts`'s collect. | The 2 `.collect()` calls in `convex/webhooks.ts` (`list`, `activeSubscriptions`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `apiKeys.ts`'s `list()` (1 site, #901). Admin-provisioned API/MCP access credentials, bounded by how many keys an org's admins create (single digits to low tens), not request volume. | The 1 `.collect()` call in `list()` (`convex/apiKeys.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `assets.ts`'s `listPage`/`listGallery` each collect the org's `models`, `categories`, and `locations` tables (6 sites, #901) purely to build id→doc enrichment maps attached to the returned asset rows. All three are small, admin-curated catalog/config tables, bounded by how many models/categories/locations an org defines, not by fleet size. Excludes the `assets` collects in the same two functions — the core growable fleet table, and the unresolved half of the perf-design doc's Finding #1/T3 (denormalize + searchIndex, approved but not yet shipped); tracked separately, not registered. | The 6 `.collect()` calls on models/categories/locations in `listPage`/`listGallery` (`convex/assets.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `crewSkills.ts`'s `list()` (1 site, #901) — small, admin-curated catalog (crew certifications/skills), bounded by how many distinct skills an org defines, not headcount or transaction volume. | The 1 `.collect()` call in `list()` (`convex/crewSkills.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `customFieldDefinitions.ts`'s `list()` (1 site, #901) — an org's custom-field definitions (entityType×fieldKey config rows), bounded by how many custom fields an org configures, not record volume. | The 1 `.collect()` call in `list()` (`convex/customFieldDefinitions.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `lineItemMergeMaps.ts`'s `list()` (1 site, #901) — line-item-merge audit rows (one per merge-reconciliation event, not per line item), low-volume by construction. Service-only, no reactive subscription. | The 1 `.collect()` call in `list()` (`convex/lineItemMergeMaps.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `suppliers.ts`'s `list`/`listPage` (small, admin-curated supplier catalog, bounded by how many suppliers a company deals with) and `assetsPage`/`subhiresPage`'s `models` enrichment (attaching a model name to an already-paginated page) — 4 sites (#901). Excludes the `assets`/`supplierOrders`/`projects` collects in `counts`/`subhiresPage` — growable transactional tables, tracked separately as a bounding follow-up (existing `by_supplierId`/`by_cuid` indexes apply). | The 4 `.collect()` calls at `convex/suppliers.ts` (`list`, `listPage`, `assetsPage`, `subhiresPage` model-enrichment) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `tags.ts`'s `getOrgTags()` (1 site spanning 9 table scans, #901) computes the distinct union of every taggable entity's `tags` array (models/assets/bulkAssets/kits/locations/categories/maintenanceRecords/projects/clients) for org-wide tag autocomplete. Genuine whole-set aggregation: no index can shortcut "the distinct values inside a string-array field" across heterogeneous tables. Explicitly one-shot only (autocomplete on mount/org-change), never a reactive subscription. If per-org counts show up in slow-query telemetry, the fix is a materialized/write-time tag registry, not pagination (which would silently miss tags outside the fetched page). | The `.collect()` calls inside `getOrgTags()` (`convex/tags.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `warehouseOps.ts`'s `defaultLocationId`/`checkinKitFull`/`checkinKitsBatchCore`/`checkinItemsCore` (4 sites, #901) each collect the org's `locations` table to find the default warehouse/storage location for check-in returns. `locations` is a small, admin-curated config table (typically single digits to low tens), not a transaction-volume table. | The 4 `.collect()` calls on `locations` in `convex/warehouseOps.ts` (`defaultLocationId`, `checkinKitFull`, `checkinKitsBatchCore`, `checkinItemsCore`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `testTagAssets.ts`'s `listPage` and `dashboardStats` (2 sites, #901 round-4 follow-up) each need the org's full `testTagAssets` set materialized in JS: `listPage` combines free-text search across 5 fields with arbitrary-column sort (no index shortcuts that combination); `dashboardStats` computes status tallies where `status` is `v.optional(...)` (schema.ts) — a per-status indexed scan can't provably enumerate an undefined-status row, so it would silently under-count. Both were converted from a bare `.collect()` to `collectCapped()` (`convex/lib/pagination.ts`) so a pathological row count is bounded rather than unbounded; the rest of each function's downstream reads (record counts, asset/bulkAsset joins, recent-tests) were narrowed to the displayed page/candidates only via existing indexes (`by_organizationId_testTagAssetId`, `by_organizationId_testDate`, `by_cuid`), not the whole org. | The 2 `collectCapped()` calls on `testTagAssets` in `convex/testTagAssets.ts` (`listPage`, `dashboardStats`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `wooCommerceOrderLogs.ts`'s `list()` (1 site, #901 round-4 follow-up), whose sole remaining caller (`getWooCommerceOrderLogsPage`, the admin order-log page) needs the org's full matching set to filter by status, sort by `createdAt` desc, and paginate in JS — no composite `(status, createdAt)` index exists yet. Converted from a bare `.collect()` to `collectCapped()` so a pathological row count is bounded, not unbounded; a real cursor-`.paginate()` migration would need that composite index and a caller-facing API change (page/pageSize → cursor), deferred as a larger follow-up. The webhook idempotency check (`findCompletedOrderLog`) that used to share this same whole-org collect was NOT registered here — it's fixed for real via the new `findCompletedByOrder` query (`by_wooOrderId` index). | The 1 `collectCapped()` call in `list()` (`convex/wooCommerceOrderLogs.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `assetScanLogs.ts`'s `list()` (1 site, #901 round-4 follow-up), whose sole remaining caller (`getScanLog`'s no-filter fallback path) needs the org's full set to sort by `scannedAt` desc and slice an arbitrary requested page number — not a forward-only cursor, so `.paginate()` can't directly replace it without a caller-facing API change (page/pageSize → cursor). The `assetId`/`projectId`-filtered paths in the same caller already use the most selective index available (`listByOrgAndAsset`/`listByProject`) and are unaffected. Converted from a bare `.collect()` to `collectCapped()` so a pathological row count is bounded, not unbounded. | The 1 `collectCapped()` call in `list()` (`convex/assetScanLogs.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `kits.ts`'s `availableAssets`/`availableBulkAssets` (2 sites, #901 round-4 follow-up) each need the org's full `assets`/`bulkAssets` set: the availability filter treats an absent `status` as AVAILABLE/ACTIVE (`a.status ?? "AVAILABLE"`), so a `by_organizationId_status` indexed scan for one specific status value can't safely stand in without also covering the undefined case. Converted from a bare `.collect()` to `collectCapped()` so a pathological fleet size is bounded, not unbounded. | The 2 `collectCapped()` calls in `availableAssets`/`availableBulkAssets` (`convex/kits.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `assetAccessories.ts`'s `availableSerialized` (2 sites, #901 round-4 follow-up): the `assets` collect has the same absent-status-defaults-to-AVAILABLE constraint as the `kits.ts` row above; the `assetBulkChildren` collect computes `parentIds` — a reverse aggregation ("which asset ids does any bulk-child point at as its parent") that genuinely needs every org row, not a per-id lookup. Both converted from a bare `.collect()` to `collectCapped()`. | The 2 `collectCapped()` calls in `availableSerialized` (`convex/assetAccessories.ts`) | Jayden (eng) | 2026-10-25 |
| R-8.3.3 | `projectLineItems.ts`'s `list()` (1 site, #901 core-table tier, docs/designs/perf-convex-efficiency-2026-06.md Finding #0b follow-up). Its two live-traffic callers (`notification-email-sender.ts`, `notifications.ts` flagged-asset digest scans) were moved off this whole-org read onto a new bounded `listFlagged` query (`by_organizationId_prepStatus` index, capped `.take(limit)` per status). The one remaining caller, `site-admin.ts`'s user-deletion sweep, genuinely needs every org line item to find and clear `checkedOutById`/`returnedById` FK references to the deleted user across the whole org — a paginated/bounded read would silently miss rows outside the fetched page, leaving dangling FKs to a deleted user. Runs on the rare admin user-delete path, not a hot request path. | The 1 `.collect()` call in `list()` (`convex/projectLineItems.ts`) | Jayden (eng) | 2026-10-26 |
| R-8.3.3 | `assets.ts`'s `list()` (1 site, #901 core-table tier). No longer used as a browser subscription (`useAssets` was retired when `asset-table.tsx`/`asset-gallery.tsx` moved to `listPage`/`listGallery` — only a stray code comment referenced it, no live callers found). Its 2 remaining real callers, `getAssetsByOrg`/`getMappedAssetsByOrg` (`src/lib/assets-read.ts`), are one-shot server-side reads feeding ~11 cross-domain composites (T&T, scan, check, damage, line-item composition, notifications, the PDF pipeline) that stay on the always-fresh Convex mirror until Prisma decommission (per the module's own docstring) — genuinely need the whole registry, not a page of it. | The 1 `.collect()` call in `list()` (`convex/assets.ts`) | Jayden (eng) | 2026-10-26 |
| R-8.3.3 | `assets.ts`'s `listPage`/`listGallery` core `assets` collects (2 sites, #901 core-table tier). This is the perf-design doc's own **T3 decision** (denormalize `model.name`/`categoryId` onto the asset row + use the existing `search_name`/`search_manufacturer`/`search_assetTag` search indexes) — **approved but not yet implemented**; both queries still `.collect()` the whole org's assets and filter/sort/paginate in JS, server-side. Real fix needs a new denormalization mirror (sync-on-model-rename + backfill + test) and doesn't fully compose even then: the filter set (status × condition × location × category × tags) doesn't reduce to one searchIndex's `filterFields`. That's a multi-PR architecture project, not a mechanical bounding fix — tracked as its own follow-up initiative rather than folded into this cleanup (`listGallery`'s "browse everything" read shape is a separate, already-accepted tradeoff — see the adjacent Option-A row). | The 2 `.collect()` calls on `assets` in `listPage`/`listGallery` (`convex/assets.ts`) | Jayden (eng) | 2026-10-26 |
| R-8.3.3 | `assets.ts`'s `listGallery` (1 site, #901 core-table tier + docs/designs/perf-convex-efficiency-2026-06.md "Option A" decision). Deliberately unpaginated: the gallery groups every active asset by category on one scroll, which doesn't fit a paginated contract — "show everything" is the feature, not a leftover bug. Infinite-scroll is the real fix if/when org asset counts make this matter; revisit then. | The 1 `.collect()` call in `listGallery` (`convex/assets.ts`) | Jayden (eng) | 2026-10-26 |
| R-8.3.3 | `projects.ts`'s `list()` (1 site, #901 core-table tier). Two real callers: `getProjectsByOrg` (`src/lib/projects-read.ts`), a one-shot server-side read feeding ~12 cross-domain composites (woocommerce, warehouse, sub-hires, notifications, crew-time, the iCal feed) that stay on the always-fresh Convex mirror until Prisma decommission, same shape as the `assets.ts` row above; and `clients-dashboard.tsx`'s `useProjects`, which computes org-wide rollup tiles (active-client count, clients-with-open-work, total project value, most-common client type) across every client and every project — not scoped to one client (the design doc's "per-client" framing doesn't match current code; there's no `clientId` to narrow by, so `projects.by_clientId` doesn't apply). Moving the reduction into a server-side Convex aggregate would shrink the browser payload but wouldn't reduce the underlying collect — out of scope for this read-bounding cleanup. | The 1 `.collect()` call in `list()` (`convex/projects.ts`) | Jayden (eng) | 2026-10-26 |
| R-8.3.3 | `projects.ts`'s `listPage` core `projects` collect (1 site, #901 core-table tier). Same T3 shape as the `assets.ts` row above (approved, unshipped denormalize+searchIndex rework) — still `.collect()`s the whole org's projects and filters/sorts/paginates in JS. `projects` has no search index at all yet (unlike `assets`/`models`/`suppliers`/`clients`), so this needs a schema change too. Tracked as its own follow-up initiative, not folded into this cleanup. | The 1 `.collect()` call on `projects` in `listPage` (`convex/projects.ts`) | Jayden (eng) | 2026-10-26 |
| R-8.3.3 | `projects.ts`'s `listBoard` (1 site, #901 core-table tier + perf-design doc "Option A" decision). Deliberately unpaginated, same shape as `assets.ts`'s `listGallery` row above: the kanban board groups every non-template, non-cancelled project into status columns on one screen — "show everything" is the feature. Revisit with infinite-scroll/virtualization if org project counts make this matter. | The 1 `.collect()` call in `listBoard` (`convex/projects.ts`) | Jayden (eng) | 2026-10-26 |
