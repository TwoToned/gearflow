/**
 * Default org branding colours — the single place these hex values are
 * literal (POLICY.md R-8.7.1's "brand-default config constants" exemption),
 * so `BrandingSettings` (Settings page) and the wizard's `StepBranding`
 * agree on what "unchanged from default" means when deciding whether to
 * persist a colour at all (R-3.1 — one definition, not two hand-kept copies).
 */
export const DEFAULT_PRIMARY_COLOR = "#0d4f4f";
export const DEFAULT_ACCENT_COLOR = "#10b981";
export const DEFAULT_DOCUMENT_COLOR = "#0d4f4f";
